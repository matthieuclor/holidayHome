if ($("#family-modal").length > 0) {
  $("#family-modal").modal("hide")
}

$("#modal-container").html("<%= j render "form" %>")

<% if flash.present? %>
  $("#toast-container").prepend("<%= j render "shared/flash_messages" %>")
  <% flash.clear %>
<% end %>

$("#family-modal").modal("show")
