<div class="d-flex justify-content-center">
  <div class="col-11">
    <div class="d-flex align-content-center my-4">
      <h1 class="m-0 mr-auto">Familles</h1>
      <%= button_to "Réinitialiser",
                    admin_account_families_path,
                    method: :get,
                    id: "reset-filter",
                    class: "btn btn-danger m-2 d-none" %>

      <div class="col-3 p-0">
      <%= search_form_for [:admin_account, @query], wrapper: :inline_form, remote: true do |f| %>
          <%= f.input :name_cont,
                      as: :string,
                      required: false,
                      input_html: {
                        class: "my-2",
                        placeholder: "Rechercher une famille"
                      } %>
      <% end %>
      </div>
    </div>
    <div class="card">
      <div class="card-body">
        <div id="families-table-container">
          <%= render "families_table" %>
        </div>
      </div>
    </div>
  </div>
</div>
